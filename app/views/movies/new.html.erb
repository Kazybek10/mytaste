<div class="container-new-movie p-5 my-5">
  <h1>Add New Movie</h1>

  <% if @movie.errors.any? %>
    <div id="error_explanation" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4">
      <strong class="font-bold">Object could not be saved:</strong>
      <ul class="mt-2 list-disc list-inside">
        <% @movie.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= simple_form_for @movie do |f| %>
    <%= f.input :title, label: "Movie Title" %>
    <%= f.input :description, as: :text, label: "Description" %>
    <%= f.input :release_year, label: "Release Year" %>
    <%= f.input :director, label: "Director" %>

    <div class="form-actions mt-4">
      <%= f.button :submit, "Add Movie", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% end %>
</div>